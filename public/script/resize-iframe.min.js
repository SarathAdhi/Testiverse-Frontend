window.addEventListener("message", function (event) {
  if (event.data.type === "embedHeight") {
    const embeddedIframe = document.getElementById("embeddedIframe");

    if (embeddedIframe) {
      embeddedIframe.style.height = event.data.height + "px";
    }
  }
});
